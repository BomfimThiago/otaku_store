{
  "name": "minion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Unsupervised orchestrator of coding agents — see SPEC.md",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "echo \"no tests yet\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5"
  }
}
